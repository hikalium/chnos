#C言語アプリ用Makefile
#アプリ設定
APP      = star1
STACK    = 1k
MALLOC   = 40k

include ../app_make.txt

clean :
	-$(DEL) *.lst
	-$(DEL) *.obj
	-$(DEL) *.map
	-$(DEL) *.bim
	-$(DEL) *.ca
	-$(DEL) $(APP).nas
	-$(DEL) *.gas
	-$(DEL) chnos.img
